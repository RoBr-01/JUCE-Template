{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Launch AudioPluginHost",
      "type": "shell",
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": [],
      "options": {
        "env": {
          "PATH": "${env:PATH}"
        }
      },
      "osx": {
        "command": "open",
        "args": [
          "-a",
          "${config:AUDIOPLUGINHOST_PATH}"
        ]
      },
      "windows": {
        "command": "${config:AUDIOPLUGINHOST_PATH}",
        "args": []
      },
      "linux": {
        "command": "${config:AUDIOPLUGINHOST_PATH}",
        "args": []
      }
    }
  ]
}